{% extends 'DmAdminBundle::layout.html.twig' %}

{% block breabcrumb %}
<div class="page-bar">
    <ul class="page-breadcrumb">
        <li>
            <i class="fa fa-dashboard"></i> <a href="{{ path('dashboard')}}">控制台</a>
        </li>
        <li><i class="fa fa-angle-right"></i></li>
        <li>
            <a href="{{ path('admin_carousel')}}">轮播图管理</a>
        </li>
        <li><i class="fa fa-angle-right"></i></li>
        <li>
            <span>轮播图查看</span>
        </li>
    </ul>
</div>
{% endblock %}

{% block content_wrapper -%}
<div class="row">
    <div class="col-md-12">
        <div class="portlet light bordered">
            <div class="portlet-title">
                <div class="caption">
                    <i class="icon-docs font-green"></i>
                    <span class="caption-subject font-green sbold uppercase">轮播图查看</span>
                </div>
                <div class="actions">
                    <a href="{{ path('admin_carousel') }}" class="btn btn-sm green"><i class="fa fa-reply"></i> 返回轮播图列表</a>
                    <a href="{{ path('admin_carousel_edit', {id: entity.id}) }}" class="btn btn-sm default"><i class="fa fa-edit"></i> 编辑</a>
                    <a href="javascript:;" class="btn btn-sm red delete-link" data-did="{{ entity.id }}"><i class="fa fa-trash-o"></i> 删除</a>
                </div>
            </div>
            <div class="portlet-body">
                <table class="table table-striped table-bordered">
                    <tbody>
                        <tr>
                            <td style="width: 20%;">图片</td>
                            <td><img style="width: 100%;" src="{{ entity.picture.webPath }}" /></td>
                        
                        </tr>
                        <tr>
                            <td style="width: 20%;">轮播组</td>
                            <td>{{ entity.carouselGroup.name }}</td>
                        </tr>
                        <tr>
                            <td style="width: 20%;">排序</td>
                            <td>{{ entity.sequence }}</td>
                        </tr>
                        <tr>
                            <td style="width: 20%;">链接</td>
                            <td>{{ entity.link }}</td>
                        </tr>
                    </tbody>
                </table>
                {{ form_delete(delete_form, entity.id) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
